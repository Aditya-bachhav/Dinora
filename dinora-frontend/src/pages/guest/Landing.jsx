import { useState } from 'react';
import { Link } from 'react-router-dom';
import { Card } from '../../components/Ui';
export default function Landing(){const[t,setT]=useState('');return <div className="landing"><div className="landing-mark"><b>Dinora</b><small>Digital Dine-in</small></div><div className="landing-copy"><span>PHASE 1</span><h1>Table ordering, without the waiter chase.</h1><p>Scan a table QR to open the restaurant menu. For testing, you can open a table link directly.</p></div><Card><form className="form-grid" onSubmit={e=>{e.preventDefault();if(t.trim())location.href=`/t/${t.trim()}`}}><label>Table token<input required placeholder="table-4" value={t} onChange={e=>setT(e.target.value)}/></label><button className="btn primary large">Open table</button></form><p className="muted small-text">The admin creates the table and its QR code. Scanning that QR opens the table confirmation screen.</p></Card><Link className="text-link" to="/admin/login">Restaurant admin</Link></div>}
